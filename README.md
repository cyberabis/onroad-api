Before running server.js, set environment variables:

If using Service Bus Topic: export NS_KEY=<Service Bus namespace Access Key>

If using Service Bus Topic: export export EVENTHUBS_KEY=<Event Hubs Access Key>